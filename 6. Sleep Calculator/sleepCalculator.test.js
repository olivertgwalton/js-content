const {
    getSleepHours,
    getActualSleepHours,
    getIdealSleepHours,
    calculateSleepDebt
} = require("./sleepCalculator")

// This is just a placeholder to prevent test suite errors
describe.skip('', () => {
    test('', () => { });
});